---
layout: base.njk
title: News
description: All the latest news and updates from Inch Island, Inishowen, Co. Donegal.
permalink: /news/
---

<!-- Page Header -->
<header class="bg-white border-b border-gray-200">
  <div class="max-w-6xl mx-auto px-4 py-12 md:py-16">
    <h1 class="font-display text-4xl md:text-5xl text-gray-900 mb-3">All News</h1>
    <p class="text-lg text-gray-600">Stories and updates from Inch Island and the surrounding area.</p>
  </div>
</header>

<section class="max-w-6xl mx-auto px-4 py-12">

  {% set posts = collections.posts %}

  {% if posts.length > 0 %}

  <!-- Articles List -->
  <div class="space-y-0">
    {% for post in posts %}
    <article class="group border-b border-gray-200 py-8 first:pt-0 last:border-0">
      <a href="{{ post.url }}" class="grid md:grid-cols-12 gap-4 md:gap-8 items-start">

        <!-- Date Column -->
        <div class="md:col-span-2 text-sm text-gray-500">
          <time datetime="{{ post.data.date }}">{{ post.data.date | dateFormat }}</time>
        </div>

        <!-- Content Column -->
        <div class="md:col-span-10">
          <!-- Category -->
          <span class="text-xs font-semibold uppercase tracking-wider
            {% if post.data.category == 'Nature' %}text-category-nature
            {% elif post.data.category == 'Council' %}text-category-council
            {% elif post.data.category == 'Community' %}text-category-community
            {% elif post.data.category == 'Business' %}text-category-business
            {% elif post.data.category == 'Events' %}text-category-events
            {% else %}text-ocean-600{% endif %}
          ">
            {{ post.data.category or 'News' }}
          </span>

          <!-- Title -->
          <h2 class="font-display text-2xl md:text-3xl text-gray-900 leading-tight mt-2 mb-3 group-hover:text-ocean-700 transition-colors">
            {{ post.data.title }}
          </h2>

          <!-- Excerpt -->
          {% if post.data.excerpt %}
          <p class="text-gray-600 leading-relaxed mb-3">
            {{ post.data.excerpt }}
          </p>
          {% endif %}

          <!-- Source -->
          {% if post.data.source %}
          <p class="text-sm text-gray-500">
            via {{ post.data.source }}
          </p>
          {% endif %}
        </div>

      </a>
    </article>
    {% endfor %}
  </div>

  {% else %}

  <!-- No posts -->
  <div class="text-center py-16">
    <div class="font-display text-6xl text-gray-300 mb-4">📰</div>
    <h2 class="font-display text-2xl text-gray-900 mb-2">No news yet</h2>
    <p class="text-gray-500">Check back soon for updates about Inch Island.</p>
  </div>

  {% endif %}

</section>
